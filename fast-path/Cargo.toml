[package]
name = "tempera-browser-fastpath"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Persistent JSONL fast-path gateway for Tempera Browser"

[dependencies]
serde_json = "1"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = "symbols"
